diff -ruN base_v131/release/podkop-telegram-agent/README.md work_v132/release/podkop-telegram-agent/README.md
--- base_v131/release/podkop-telegram-agent/README.md	2026-05-23 21:59:10.000000000 +0000
+++ work_v132/release/podkop-telegram-agent/README.md	2026-05-24 05:34:01.260081805 +0000
@@ -1,8 +1,8 @@
 # podkop-telegram-agent
 
-Latest fix: v1.0.30 — Support Bundle, Tunnel Health, Active Outbound Probe, Admins, bot transport and Podkop section switch.
+Latest fix: v1.0.32 — Quiet hours for night notifications.
 
-Версия в интерфейсе: **Podkop Telegram Agent v1.0.8**
+Версия в интерфейсе: **Podkop Telegram Agent v1.0.9**
 
 Telegram-модуль для Podkop/OpenWrt.
 
@@ -17,6 +17,7 @@
 ## Возможности
 
 - Telegram-панель для одного или нескольких роутеров.
+- Тихие часы уведомлений: ночные auto-alerts можно отложить до утра или не сохранять.
 - Режимы:
   - `standalone` — один роутер.
   - `panel` — главный роутер с Telegram-панелью и выбором роутеров.
diff -ruN base_v131/release/podkop-telegram-agent/RELEASE_NOTES.md work_v132/release/podkop-telegram-agent/RELEASE_NOTES.md
--- base_v131/release/podkop-telegram-agent/RELEASE_NOTES.md	2026-05-23 21:59:10.000000000 +0000
+++ work_v132/release/podkop-telegram-agent/RELEASE_NOTES.md	2026-05-24 05:34:01.260635129 +0000
@@ -1,3 +1,10 @@
+## v1.0.32 - quiet hours for alerts
+- Visible version changed to `Podkop Telegram Agent v1.0.9`.
+- Added configurable night quiet hours for automatic Telegram alerts.
+- LuCI fields: enable quiet hours, start time, end time and mode.
+- Default: 23:00-08:00, queue alerts and deliver them after quiet hours.
+- Manual Telegram commands and daily reports are not blocked.
+
 ## v1.0.30 - feature pack
 - Added `🧰 Support Bundle`: sends masked diagnostics archive with UCI configs, routes, nft rules and logs.
 - Added `🩺 Tunnel Health`: checks Podkop, sing-box, DNS/FakeIP, SOCKS/Mixed inbound and Telegram reachability.
diff -ruN base_v131/release/podkop-telegram-agent/files/etc/config/podkop_tg work_v132/release/podkop-telegram-agent/files/etc/config/podkop_tg
--- base_v131/release/podkop-telegram-agent/files/etc/config/podkop_tg	2026-05-23 21:57:20.000000000 +0000
+++ work_v132/release/podkop-telegram-agent/files/etc/config/podkop_tg	2026-05-24 05:33:20.958888290 +0000
@@ -11,6 +11,10 @@
         option check_interval '60'
         option log_lines '200'
         option alerts '1'
+        option alert_quiet_hours '1'
+        option alert_quiet_start '23:00'
+        option alert_quiet_end '08:00'
+        option alert_quiet_mode 'queue'
         option daily_report '1'
         option daily_report_time '09:00'
         option daily_backup '1'
diff -ruN base_v131/release/podkop-telegram-agent/files/usr/bin/podkop-telegram-agent work_v132/release/podkop-telegram-agent/files/usr/bin/podkop-telegram-agent
--- base_v131/release/podkop-telegram-agent/files/usr/bin/podkop-telegram-agent	2026-05-24 05:22:14.000000000 +0000
+++ work_v132/release/podkop-telegram-agent/files/usr/bin/podkop-telegram-agent	2026-05-24 05:33:20.958413665 +0000
@@ -5,7 +5,8 @@
 # ROUTER_NAME_HARDFIX_V129: preserve router names and repair uppercase-to-underscore corruption.
 # FEATURE_PACK_V130: Support Bundle, Tunnel Health, Active Outbound Probe, Admins, update info, bot transport, Podkop section switch.
 # URLTEST_REAL_FIX_V131: robust Podkop URLTest/VLESS UCI list handling and apply flow.
-BOT_VERSION="v1.0.8"
+# QUIET_HOURS_V132: configurable night quiet hours for automatic alerts.
+BOT_VERSION="v1.0.9"
 
 CONFIG="podkop_tg"
 SECTION="main"
@@ -55,6 +56,10 @@
     ROUTERS_LIST="$(uci -q get "$CONFIG.$SECTION.routers")"
 ROUTER_ALIASES="$(uci_config_list_values "$CONFIG" "$SECTION" router_aliases; uci -q get "$CONFIG.$SECTION.router_aliases" 2>/dev/null)"
     ALERTS="$(uci -q get "$CONFIG.$SECTION.alerts")"
+    ALERT_QUIET_HOURS="$(uci -q get "$CONFIG.$SECTION.alert_quiet_hours")"
+    ALERT_QUIET_START="$(uci -q get "$CONFIG.$SECTION.alert_quiet_start")"
+    ALERT_QUIET_END="$(uci -q get "$CONFIG.$SECTION.alert_quiet_end")"
+    ALERT_QUIET_MODE="$(uci -q get "$CONFIG.$SECTION.alert_quiet_mode")"
     PING_HOST="$(uci -q get "$CONFIG.$SECTION.ping_host")"
     FOREIGN_HOLD_SECONDS="$(uci -q get "$CONFIG.$SECTION.foreign_hold_seconds")"
     DEVICE_CONTROL="$(uci -q get "$CONFIG.$SECTION.device_control")"
@@ -83,6 +88,13 @@
     case "$BOT_MODE" in standalone|panel|worker) ;; *) BOT_MODE="$DEFAULT_BOT_MODE" ;; esac
     [ -z "$PANEL_SEND_HEADER_ON_ACTIONS" ] && PANEL_SEND_HEADER_ON_ACTIONS=1
     [ -z "$ALERTS" ] && ALERTS=1
+    [ -z "$ALERT_QUIET_HOURS" ] && ALERT_QUIET_HOURS=1
+    [ -z "$ALERT_QUIET_START" ] && ALERT_QUIET_START="23:00"
+    [ -z "$ALERT_QUIET_END" ] && ALERT_QUIET_END="08:00"
+    [ -z "$ALERT_QUIET_MODE" ] && ALERT_QUIET_MODE="queue"
+    case "$ALERT_QUIET_MODE" in queue|drop) ;; *) ALERT_QUIET_MODE="queue" ;; esac
+    ALERT_QUIET_START="$(normalize_time_hhmm "$ALERT_QUIET_START")"
+    ALERT_QUIET_END="$(normalize_time_hhmm "$ALERT_QUIET_END")"
     [ -z "$PING_HOST" ] && PING_HOST="1.1.1.1"
     [ -z "$DEVICE_CONTROL" ] && DEVICE_CONTROL=1
     [ -z "$TELEGRAM_TRANSPORT" ] && TELEGRAM_TRANSPORT="auto"
@@ -106,6 +118,66 @@
     sed "s/${esc}\[[0-9;?]*[ -/]*[@-~]//g"
 }
 
+normalize_time_hhmm() {
+    local t="$1" h m
+    [ -z "$t" ] && t="00:00"
+    case "$t" in
+        [0-9]:[0-5][0-9]) t="0$t" ;;
+        [0-2][0-9]:[0-5][0-9]) ;;
+        *) printf '00:00'; return ;;
+    esac
+    h="${t%:*}"
+    m="${t#*:}"
+    h="${h#0}"; [ -z "$h" ] && h=0
+    m="${m#0}"; [ -z "$m" ] && m=0
+    if [ "$h" -ge 0 ] 2>/dev/null && [ "$h" -le 23 ] 2>/dev/null && [ "$m" -ge 0 ] 2>/dev/null && [ "$m" -le 59 ] 2>/dev/null; then
+        printf '%02d:%02d' "$h" "$m"
+    else
+        printf '00:00'
+    fi
+}
+
+hhmm_to_minutes() {
+    local t h m
+    t="$(normalize_time_hhmm "$1")"
+    h="${t%:*}"
+    m="${t#*:}"
+    h="${h#0}"; [ -z "$h" ] && h=0
+    m="${m#0}"; [ -z "$m" ] && m=0
+    echo $((h * 60 + m))
+}
+
+alert_quiet_active() {
+    local now start end now_m start_m end_m
+    [ "$ALERT_QUIET_HOURS" = "1" ] || return 1
+    start="$(normalize_time_hhmm "${ALERT_QUIET_START:-23:00}")"
+    end="$(normalize_time_hhmm "${ALERT_QUIET_END:-08:00}")"
+    [ "$start" = "$end" ] && return 1
+    now="$(date '+%H:%M')"
+    now_m="$(hhmm_to_minutes "$now")"
+    start_m="$(hhmm_to_minutes "$start")"
+    end_m="$(hhmm_to_minutes "$end")"
+    if [ "$start_m" -lt "$end_m" ] 2>/dev/null; then
+        [ "$now_m" -ge "$start_m" ] 2>/dev/null && [ "$now_m" -lt "$end_m" ] 2>/dev/null
+    else
+        [ "$now_m" -ge "$start_m" ] 2>/dev/null || [ "$now_m" -lt "$end_m" ] 2>/dev/null
+    fi
+}
+
+alert_quiet_summary() {
+    local start end mode state
+    start="$(normalize_time_hhmm "${ALERT_QUIET_START:-23:00}")"
+    end="$(normalize_time_hhmm "${ALERT_QUIET_END:-08:00}")"
+    mode="${ALERT_QUIET_MODE:-queue}"
+    case "$mode" in drop) mode="не сохранять" ;; *) mode="отложить до утра" ;; esac
+    if [ "$ALERT_QUIET_HOURS" = "1" ]; then
+        if alert_quiet_active; then state="сейчас активно"; else state="сейчас не активно"; fi
+        printf 'Тихие часы уведомлений: <code>%s-%s</code>, <code>%s</code>, <code>%s</code>' "$start" "$end" "$mode" "$state"
+    else
+        printf 'Тихие часы уведомлений: <code>выключены</code>'
+    fi
+}
+
 router_header() {
     printf '[%s]' "$ROUTER_NAME" | escape_html
 }
@@ -1304,6 +1376,7 @@
 Роутер: <code>$(printf '%s' "$ROUTER_NAME" | escape_html)</code>
 Hostname: <code>$(printf '%s' "$host" | escape_html)</code>
 Время: <code>${now}</code>
+$(alert_quiet_summary)
 
 <b>🧠 Система</b>
 ${system_info}
@@ -3556,6 +3629,10 @@
 <code>/accessunblock ${router_html} tv</code> — снять ручную блокировку
 <code>/accessdel ${router_html} tv</code> — удалить правило расписания
 <code>/testreport ${router_html}</code> — тест ежедневного отчёта сейчас
+
+Ночные уведомления настраиваются в LuCI: тихие часы, начало, конец и режим — отложить до утра или не сохранять.
+Текущее: $(alert_quiet_summary)
+
 <code>/donate ${router_html}</code> — донат скриптеру
 <code>/header ${router_html}</code> — проверить картинку-шапку меню
 
@@ -4253,6 +4330,16 @@
 
 send_alert_bundle() {
     local kind="$1" title="$2" message="$3" logs_title="$4" logs_body="$5"
+    if alert_quiet_active; then
+        if [ "$ALERT_QUIET_MODE" = "drop" ]; then
+            log "alert suppressed by quiet hours kind=$kind title=$title window=${ALERT_QUIET_START}-${ALERT_QUIET_END} mode=drop"
+            return 0
+        fi
+        queue_alert_event "$kind" "$title" "$message" "$logs_title" "$logs_body"
+        log "alert postponed by quiet hours kind=$kind title=$title window=${ALERT_QUIET_START}-${ALERT_QUIET_END} mode=queue"
+        return 1
+    fi
+
     if send_all_admins "$message" && send_all_admins_plain_long "$logs_title" "$logs_body"; then
         return 0
     fi
@@ -4265,6 +4352,10 @@
     [ "$ALERTS" = "1" ] || return
     [ -d "$ALERT_QUEUE_DIR" ] || return
     [ -n "$(get_admin_chats)" ] || return
+    if alert_quiet_active; then
+        log "alert queue flush postponed by quiet hours window=${ALERT_QUIET_START}-${ALERT_QUIET_END}"
+        return
+    fi
     internet_runtime_ready || return
 
     for dir in "$ALERT_QUEUE_DIR"/*; do
diff -ruN base_v131/release/podkop-telegram-agent/files/www/luci-static/resources/view/podkop-tg/settings.js work_v132/release/podkop-telegram-agent/files/www/luci-static/resources/view/podkop-tg/settings.js
--- base_v131/release/podkop-telegram-agent/files/www/luci-static/resources/view/podkop-tg/settings.js	2026-05-24 05:21:34.000000000 +0000
+++ work_v132/release/podkop-telegram-agent/files/www/luci-static/resources/view/podkop-tg/settings.js	2026-05-24 05:33:20.959368135 +0000
@@ -3,6 +3,7 @@
 // LUCISTRIPANSI_V127: fixed LuCI strict-mode octal/control char error in stripAnsi().
 // ROUTER_NAME_HARDFIX_V129: preserve router names and repair uppercase-to-underscore corruption.
 // URLTEST_REAL_FIX_V131: Telegram URLTest/VLESS robust backend fix.
+// QUIET_HOURS_V132: configurable night quiet hours for automatic alerts.
 // FEATURE_PACK_V130: Telegram feature pack fields for Support Bundle, Tunnel Health, Probe, Admins, transport, sections.
 // OpenWrt 25 LuCI may fail with 'view.extend is not a function' if variables are placed before requires.
 'require view';
@@ -12,7 +13,7 @@
 'require ui';
 'require poll';
 
-var BOT_VERSION = 'v1.0.8';
+var BOT_VERSION = 'v1.0.9';
 
 function serviceStatus() {
     return fs.exec('/etc/init.d/podkop-telegram-agent', [ 'status' ]).then(function(res) {
@@ -424,7 +425,44 @@
         o = s.option(form.Flag, 'alerts', _('Мгновенные уведомления'));
         o.default = '1';
         o.rmempty = false;
-        o.description = _('Отправлять уведомление в Telegram, если Podkop или sing-box выключился/упал, и когда работа восстановилась.');
+        o.description = _('Отправлять уведомление в Telegram, если Podkop или sing-box выключился/упал, если WAN/интернет пропал, и когда работа восстановилась.');
+
+        o = s.option(form.Flag, 'alert_quiet_hours', _('Тихие часы уведомлений'));
+        o.default = '1';
+        o.rmempty = false;
+        o.description = _('Не отправлять автоматические уведомления ночью. Ручные команды Telegram и ежедневный отчёт не блокируются.');
+
+        o = s.option(form.Value, 'alert_quiet_start', _('Начало тихих часов'));
+        o.depends('alert_quiet_hours', '1');
+        o.placeholder = '23:00';
+        o.default = '23:00';
+        o.rmempty = false;
+        o.description = _('Формат HH:MM. Например 23:00.');
+        o.validate = function(section_id, value) {
+            if (!/^([01]?\d|2[0-3]):[0-5]\d$/.test(value || ''))
+                return _('Укажите время в формате HH:MM, например 23:00.');
+            return true;
+        };
+
+        o = s.option(form.Value, 'alert_quiet_end', _('Конец тихих часов'));
+        o.depends('alert_quiet_hours', '1');
+        o.placeholder = '08:00';
+        o.default = '08:00';
+        o.rmempty = false;
+        o.description = _('Формат HH:MM. Например 08:00. Если начало больше конца, интервал считается через полночь.');
+        o.validate = function(section_id, value) {
+            if (!/^([01]?\d|2[0-3]):[0-5]\d$/.test(value || ''))
+                return _('Укажите время в формате HH:MM, например 08:00.');
+            return true;
+        };
+
+        o = s.option(form.ListValue, 'alert_quiet_mode', _('Что делать с ночными уведомлениями'));
+        o.depends('alert_quiet_hours', '1');
+        o.value('queue', _('Отложить и отправить после тихих часов'));
+        o.value('drop', _('Не сохранять и не отправлять'));
+        o.default = 'queue';
+        o.rmempty = false;
+        o.description = _('В режиме «отложить» бот не шумит ночью, но утром отправит накопленные уведомления и логи. В режиме «не сохранять» ночные авто-уведомления будут просто пропущены.');
 
         o = s.option(form.Flag, 'daily_report', _('Ежедневный отчёт'));
         o.default = '1';
diff -ruN base_v131/release/podkop-telegram-agent/install.sh work_v132/release/podkop-telegram-agent/install.sh
--- base_v131/release/podkop-telegram-agent/install.sh	2026-05-24 05:22:51.000000000 +0000
+++ work_v132/release/podkop-telegram-agent/install.sh	2026-05-24 05:33:20.959744420 +0000
@@ -608,6 +608,10 @@
     uci_set_if_empty "$CONFIG.$SECTION.check_interval" "60"
     uci_set_if_empty "$CONFIG.$SECTION.log_lines" "40"
     uci_set_if_empty "$CONFIG.$SECTION.alerts" "1"
+    uci_set_if_empty "$CONFIG.$SECTION.alert_quiet_hours" "1"
+    uci_set_if_empty "$CONFIG.$SECTION.alert_quiet_start" "23:00"
+    uci_set_if_empty "$CONFIG.$SECTION.alert_quiet_end" "08:00"
+    uci_set_if_empty "$CONFIG.$SECTION.alert_quiet_mode" "queue"
 
     # Daily report must work out of the box after SSH install/update.
     # Keep the user's chosen time, but ensure the feature is enabled automatically.
